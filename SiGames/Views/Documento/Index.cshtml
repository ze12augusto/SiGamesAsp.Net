@model IEnumerable<SiGames.Models.documento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdDocumento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Documento1)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.pessoa.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tipodocumento.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdDocumento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Documento1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.pessoa.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tipodocumento.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdDocumento }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdDocumento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdDocumento })
        </td>
    </tr>
}

</table>
